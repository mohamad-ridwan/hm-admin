/** @type {import('next').NextConfig} */

const nextConfig = {
    images: {
        unoptimized: true
    },
    // output: "export",
    // experimental: {
    //     serverActions: true
    // },
}

module.exports = nextConfig